<% if flash.any? %>
  <div class="row">
    <div class="col-12">
      <% flash.each do |name, message| %>
        <div class="alert alert-<%= name == 'notice' ? 'success' : 'danger' %>" role="alert">
          <%= message %>
        </div>
      <% end %>
    </div>
  </div>
<% end %>
